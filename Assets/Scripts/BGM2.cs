using UnityEngine;

public class BGM2 : MonoBehaviour
{
    private static BGM2 instance;
    void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}